/** Formats as: [SMS] {recipient}: {message} — truncate message to 160 chars. */
public class SmsChannel implements NotificationChannel {
    @Override
    public String send(String recipient, String message) {
        // TODO
        return "";
    }
}
